package unix.shell.redirect;

import unix.shell.operator.rd.RedirectionType;

public class UnixRedirection {

	private RedirectionType rdType;

	public UnixRedirection(RedirectionType rdType) {
		this.rdType = rdType;
	}
}